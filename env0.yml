version: 1

deploy:
  steps:
    terraformPlan:
      before:
        - echo "1"
      after:
        - echo "checking if env is aaa"
        - exit 1

    terraformApply:
      before:
        - |
            pip3 install pyvmomi
            python3 getallvms.py -s $HOST -o $PORT -u $USER -p $PASSWORD -S
