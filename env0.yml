version: 1

deploy:
  steps:
    terraformPlan:
      after:
        - echo "checking if env is the default environment"

    terraformApply:
      before:
        - |
            pip3 install pyvmomi
            python3 getallvms.py -s $HOST -o $PORT -u $USER -p $PASSWORD -S
